#include "main.h"
/**
 * main - check the code
 * Return: Always 0.
 */
void print_alphabet(void)
{
char message;
for (message = 'a'; message <= 'z'; message++)
    _putchar(message);
_putchar('\n');
}
